@AGENTS.md

## Claude Code

- Keep this file thin. The shared WGIS agent policy lives in `AGENTS.md`.
- For WGIS DB work under `data/`, explicitly read `data/DATA_AUTHORING_GUIDE.md` and `data/PROJECT_STATE.md` before editing.
- Run `node scripts/validate-data.js` before committing DB changes.
