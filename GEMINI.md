@./AGENTS.md

## Gemini CLI and Antigravity

- Use `AGENTS.md` as the shared project policy.
- For WGIS DB work, read `data/DATA_AUTHORING_GUIDE.md`, `data/PROJECT_STATE.md`, and `data/taxonomy.json` before editing.
- Keep each data task to one focused curation batch.
- Run `node scripts/validate-data.js` before committing DB changes.
